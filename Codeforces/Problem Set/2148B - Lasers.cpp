#include<bits/stdc++.h>
using namespace std;
#define ll long long
int main() {
	ll t;
	cin >> t;
	while(t--){
		int n, m, x, y, c;
        cin >> n >> m >> x >> y;
        for(int i = 0; i < n + m; i++) cin >> c;
        cout << n + m << endl;
	}
	return 0;
}